//First we want to capitalize the first letter of a string

const tocapitalize({
    return tocapitalizeFirstLetter.charAt(0).toUpperCase + tocapitalizeFirstLetter.splice(1)
})


function tocapitalizeFirstLetterMap(){
   
    const newLetter = tocapitalizelizeFirstLetter.split("")
    console.log(newLetter)
    const result = newLetter.map((char, index) => {
        if (index ===num) {
            return char.
        }
    })
}




//reverse string temitope = epotimet
const reverseString = "temitope"


function reverseStringFunction(){
    const newLetters = reverseString.split("")
    return newLetters.reverse().join

}
reverseStringFunction()